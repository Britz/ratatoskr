# ============================================================
#  OEFFENTLICHES Repo (britz/ratatoskr) = nur die fertig gebaute Site.
#  Inhalt wird im PRIVATEN Repo aus dem Vault gebaut und nach www/ gepusht
#  (tools/publish_site.sh). www/ WIRD getrackt — nicht ignorieren!
#  Hier landet NIE der Vault, nie (c)-Quellen, nie Kaufdaten.
# ============================================================

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
